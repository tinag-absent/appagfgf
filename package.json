{
  "name": "kaishoku-agency",
  "version": "1.0.0",
  "private": true,
  "description": "海蝕機関 — ARG Web Application",
  "scripts": {
    "minify-json":    "node scripts/minify-json.js",
    "validate-json":  "node scripts/validate-json.js",
    "check-relations":"node scripts/check-relations.js",
    "lint":           "npm run validate-json && npm run check-relations",
    "build":          "npm run lint && npm run minify-json",
    "dev":            "echo 'Open index.html in browser'"
  },
  "devDependencies": {
    "ajv":        "^8.12.0",
    "ajv-formats": "^2.1.1"
  }
}
